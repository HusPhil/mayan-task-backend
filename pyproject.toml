[project]
name = "mayan-task-management"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "HusPhil009", email = "husphil200@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "dotenv>=0.9.9",
    "fastapi>=0.141.1",
    "pydantic>=2.13.4",
    "uvicorn>=0.52.3",
]

[project.scripts]
mayan-task-management = "mayan_task_management:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"

[tool.uv]
package = false
